# Execution Log: scratch/02.1-base-algorithm.md

Started: 2026-04-22
Plan phases: 10

## Pre-flight
- Branch: `in/feat/rust-api` (HEAD `f7655bb`)
- Uncommitted diff (non-blocking, unrelated to plan):
  - `.gitignore` adds `output.txt`
  - `Cargo.lock` version bumps to `1.0.0-beta.5`
- Blocking open questions: none (all Q1–Q7 non-blocking per §10)
- Files verified present: `compare.rs`, `status.rs`, `errors.rs`,
  `corners.rs`, `corpus_parity.rs`, `simtest-funnel-cli/src/main.rs`,
  `simtest-funnel-python/python/simtest/funnel/__init__.py`,
  `tests/python/test_compare.py`, `Cargo.toml`,
  `.github/workflows/ci.yml`, `Makefile`, `pyproject.toml`,
  `docs/index.html`, `packages/sdk/src/index.ts`.
- Phase overview:
  1. Restore range-asymmetry status classification (core + py tests)
  2. `interpolate_clamped` doc audit (read-only, likely no-op)
  3. CLI emits signed errors; document LBNL divergence
  4. Tighten corpus parity tolerance
  5. Python dtype validation + `py.typed` + `.pyi` stubs
  6. Declare + CI-test MSRV 1.75
  7. Python API reference via `pdoc`
  8. TypeScript API reference via `typedoc`
  9. Documentation consistency sweep
  10. Push + PR to `beta`, watch CI

---

## Phase 1: Restore range-asymmetry status classification
**Started:** 2026-04-22
**Type:** implementation-first

### Implement
- Files modified:
  - `crates/simtest-funnel-core/src/compare.rs` — `classify_status`
    rewritten to implement Plan 02 §3.3 step 7 (Fail-wins, then
    `MissingReference` / `MissingTest` via `eps = 1e-12 × max(|t_ref_hi|, |t_test_hi|)`).
  - Existing `status_missing_reference` / `status_missing_test` unit
    tests tightened; four new tests added:
    `classify_fail_wins_over_range_mismatch`,
    `classify_missing_reference_simple`,
    `classify_missing_test_simple`,
    `classify_pass_when_ranges_align`.
  - `crates/simtest-funnel-core/tests/corpus_parity.rs` —
    `expected_status_from_c_errors` now takes `t_ref`/`t_test` and
    mirrors the new classifier (non-zero C errors inside the common
    range → Fail; else range asymmetry → Missing*; else Pass).
  - `tests/python/test_compare.py` — `test_missing_reference_status`
    and `test_missing_test_status` switched to strict `==` assertions.
- Notes: the first `cargo test --workspace` flagged `corpus_parity`
  cases `fail2` (now `MissingReference`, was `Fail`) and `fail6`
  (now `MissingTest`, was `Pass` — actually reported expected `Pass`
  under the old helper); harness updated as above.

### Validate
- `cargo test -p simtest-funnel-core compare::` — 13 passed
- `cargo test --workspace` — 31 passed
- `cargo clippy --workspace --all-targets -- -D warnings` — clean
- `uv run python -m pytest tests/python/test_compare.py::test_missing_reference_status tests/python/test_compare.py::test_missing_test_status -v` — 2 passed

### Review
- Outcome: DONE — implementation matches §3.3 step 7 exactly; all
  focused-validation commands pass.

### Focused Validation
- [x] `cargo test -p simtest-funnel-core compare::` — PASS
- [x] `cargo test --workspace` — PASS
- [x] Python range-asymmetry tests strict — PASS
- [x] `cargo clippy --workspace -- -D warnings` — PASS

**Completed:** 2026-04-22
**Result:** PASS

---

## Phase 2: `interpolate_clamped` documentation audit
**Started:** 2026-04-22
**Type:** implementation-first (docs-only)

### Implement
- Re-reading `crates/simtest-funnel-core/src/errors.rs` lines 1–16
  revealed no hard contradiction — but the module-level link pointed
  to a local absolute path `/home/iakovn/projects/funnel/src/tube.c`,
  which is a shipped-doc bug.
- Consolidated the module-level comment and the function docstring
  into one coherent narrative; replaced the local path with an
  upstream `https://github.com/lbl-srg/funnel` link.

### Validate
- `cargo doc -p simtest-funnel-core --no-deps` — exit 0, no warnings.
- `cargo test --workspace` — all tests pass.

### Review
- Outcome: DONE.

### Focused Validation
- [x] `cargo doc -p simtest-funnel-core --no-deps` clean — PASS
- [x] `cargo test --workspace` — PASS

**Completed:** 2026-04-22
**Result:** PASS

---

## Phase 3: CLI emits signed errors; document LBNL divergence
**Started:** 2026-04-22
**Type:** implementation-first

### Implement
- `crates/simtest-funnel-cli/src/main.rs` — removed `.abs()` mapping,
  wrote `result.errors.1` directly; updated `long_about` and block
  comment to point at the new "Differences from LBNL Funnel" section.
- `crates/simtest-funnel-cli/README.md` — new file documenting CLI
  usage, exit codes, and three divergences from LBNL Funnel (signed
  errors, Missing* exit codes, default tolerance).
- `crates/simtest-funnel-cli/Cargo.toml` — added `readme = "README.md"`.
- `CHANGELOG.md` — added an `Unreleased` section ahead of
  beta.5 with the BREAKING(cli) note for signed errors plus the
  Phase 1 fix(core) entry.
- `crates/simtest-funnel-core/tests/fixtures/test.csv` — bumped the
  `x=5.0` test y from `-1.0` to `-5.0` so manual CLI validation has
  at least one below-lower-bound point.

### Validate
- `cargo build -p simtest-funnel-cli --release` — success.
- Manual run against the updated fixture produced
  `errors.csv` with one negative y (`-3.994e0` at x=5.0) and exit
  code 1 (Fail). Other points are zero.
- `cargo test --workspace` — all green.
- `cargo clippy --workspace --all-targets --features simtest-funnel-core/json -- -D warnings` — clean.

### Review
- Outcome: DONE.

### Focused Validation
- [x] `cargo build -p simtest-funnel-cli --release` — PASS
- [x] Manual CLI run: `errors.csv` contains a negative value — PASS
- [x] `cargo test --workspace` — PASS

**Completed:** 2026-04-22
**Result:** PASS

---

## Phase 4: Tighten regression parity tolerance
**Started:** 2026-04-22
**Type:** implementation-first (tests + docs)

### Implement
- `crates/simtest-funnel-core/tests/corpus_parity.rs`:
  - Split `tol_eq` into `tol_errors` (Plan 02 §7.2 target
    `max(1e-12, 1e-9 × max(|a|,|b|))`, applied to error-array parity)
    and `tol_bounds` (documented relaxed envelope
    `max(1e-8, 1e-6 × max(|a|,|b|))`, applied to bound polylines).
  - Wired each caller to the appropriate tolerance.
  - Tightened the errors gate from `worst > 1e-5` to `e_bad > 0`
    (bad counts against `tol_errors`).
- `CHANGELOG.md` — added a "Tests" section to `Unreleased` recording
  the final tolerance values and the corner-algorithm follow-up.

### Validate
- Per-case `errors worst_diff` max across 17 cases is `~2.27e-14`
  (observed during investigation); all cases pass `tol_errors`.
- Bound drift is confined to `fail2`, `fail6`, `success9` (~1e-6 to
  ~1e-3), driven by the `equ = 1e-10` threshold in
  `crates/simtest-funnel-core/src/corners.rs`; retained relaxation is
  documented in-file with a pointer to plan 02.1 §10 Q1.
- `cargo test -p simtest-funnel-core --test corpus_parity --features simtest-funnel-core/json` — 2 passed.
- `cargo test --workspace` — 31 passed.
- `cargo clippy --workspace --all-targets --features simtest-funnel-core/json -- -D warnings` — clean.

### Review
- Outcome: DONE — took plan Option (c): per-column split with
  in-file documentation of the corner-algorithm caveat. Outcome (a)
  was not achievable; outcome (b) would be a larger investigation
  into the corner geometry and is tracked in §10 Q1.

### Focused Validation
- [x] `cargo test -p simtest-funnel-core --test corpus_parity` — PASS (all 17 cases)
- [x] `cargo test --workspace` — PASS

**Completed:** 2026-04-22
**Result:** PASS

---

